# Never commit local config files containing API keys
**/config.ini

# Build output
_site/

# Python cache
__pycache__/
*.pyc

# Claude Code worktrees (each is its own git checkout, never a submodule/gitlink)
.claude/worktrees/
